# Backend
# Absolute path to the generated students.csv.
# Defaults to <repo-root>/data/students.csv when unset.
DATA_PATH=

# Frontend (Vite dev-server proxy)
# URL the Vite proxy forwards /api/* requests to.
# Set to http://backend:8000 inside Docker Compose.
API_TARGET=http://localhost:8000
